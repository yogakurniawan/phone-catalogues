import styled from 'styled-components';

const DisclaimerText = styled.span`
  p {
    text-align: center;
    margin-top: 10px;
    display: block;
    font-style: italic;
  }
`;

export default DisclaimerText;
